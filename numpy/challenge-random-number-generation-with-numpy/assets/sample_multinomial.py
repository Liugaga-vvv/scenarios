import numpy as np

def sample_multinomial(probs: np.ndarray, n: int) -> np.ndarray:
    """Sample from a multinomial distribution and return the counts of the selected elements.

    Args:
        probs (np.ndarray): A NumPy array representing the probability distribution.
        n (int): The number of trials.

    Returns:
        np.ndarray: A NumPy array representing the counts of the selected elements.
    """
    # TODO: implement this function
    # Note: Do not change the existing code